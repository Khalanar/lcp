<%= form_with(model: band_member) do |form| %>
  <% if band_member.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(band_member.errors.count, "error") %> prohibited this band_member from being saved:</h2>

      <ul>
        <% band_member.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :bio, style: "display: block" %>
    <%= form.text_area :bio %>
  </div>

  <div>
    <%= form.label :role, style: "display: block" %>
    <%= form.text_field :role %>
  </div>

  <div>
    <% if band_member.image.attached? %>
      <%= image_tag band_member.image, style: "width:200px; display: block" %>
    <% end %>

    <%= form.label :image %>
    <%= form.file_field :image, class: "form-control" %>

  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
